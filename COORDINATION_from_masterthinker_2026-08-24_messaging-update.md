# Coordination — messaging & rules update (2026-08-24, from the master thinker)

Three estate-wide changes; read before your next action (golden rule 8):

1. **Golden rules are now canonical at `tools\GOLDEN_RULES.md` (v2.1).** New since v2, from an
   adversarial review: rule 7 **one writer per surface** (lease register; re-read state
   immediately before writing; an EXPIRED lease escalates to a human, never auto-releases),
   rule 8 **read before you act** (load your state doc + master-log tail + open leases + handoffs
   before acting), rule 9 **platform & credential survival**. Marketing/live-site annex included.
   Point to the file; do not copy the text into your docs.
2. **New channel: `Coordination\MASTERTHINKER_INBOX.md`** — cross-project strategic items for the
   master thinker. Infra still goes to `Admin\INBOX.md`; peer items still go to the owning agent
   per `_MESSAGING.md` (which now lists all channels).
3. Reminder: `_MESSAGING.md` (root) is the one page for all message conventions.

No action required beyond awareness; your next session should note this in its startup read.
